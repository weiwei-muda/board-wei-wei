# Wei Wei Board

This repository powers the Wei Wei board in the Awesomeree employee webapp.

The board page is defined in `index.html`. It currently contains one To Do item:

- Reroder Item

The file is intentionally static so the Awesomeree board service can fetch and render it directly from GitHub.

## Files

- `index.html` - the board page shown in the webapp.

## Status

The repository is public and ready for the Awesomeree board loader.
